version: '3.8'

services:
  sequentialthinking:
    image: hashwarlock/sequentialthinking:v0.0.1
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped